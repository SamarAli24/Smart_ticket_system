# Loaded by `vite dev` only. Points at the Kestrel https port from
# Backend/TicketSystem.Api/Properties/launchSettings.json, so run the "https" launch profile.
VITE_API_BASE_URL=https://localhost:7276/api
