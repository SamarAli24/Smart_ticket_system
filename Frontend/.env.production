# Loaded by `vite build` (which defaults to production mode).
# In production the SPA is served from the API's own wwwroot, so the API is same-origin —
# a relative base keeps requests on whatever domain the app is served from.
VITE_API_BASE_URL=/api
